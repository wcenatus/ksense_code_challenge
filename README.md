# Ksense Code Challege

## How to run

### First run 'npm install'

To start the development server run:

```bash
npm run dev
```

To create a build:

```bash
npm run build
```

To run the production build (run build first):

```bash
npm run start
```
